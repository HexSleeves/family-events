[build]
builder = "DOCKERFILE"
watchPatterns = [
  "Dockerfile",
  ".dockerignore",
  "railway.toml",
  "railway.web.toml",
  "pyproject.toml",
  "uv.lock",
  "alembic.ini",
  "README.md",
  "package.json",
  "package-lock.json",
  "main.py",
  "alembic/**",
  "src/**",
  "scripts/railway-entrypoint.sh",
]

[deploy]
startCommand            = "APP_ROLE=web ./scripts/railway-entrypoint.sh"
healthcheckPath         = "/health"
healthcheckTimeout      = 300
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 10
drainingSeconds         = 60
overlapSeconds          = 10
